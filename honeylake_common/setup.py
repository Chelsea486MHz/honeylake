from setuptools import setup, find_packages

setup(
    name="honeylake-common",
    version="1.0.0",
    description="HoneyLake framework",
    author="Chelsea486MHz",
    packages=find_packages(),
    python_requires=">=3.11",
    install_requires=[],
)
